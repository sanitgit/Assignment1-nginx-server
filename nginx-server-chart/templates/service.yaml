apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-nginx-serve.fullname" . }}
  labels:
    {{- include "my-nginx-serve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      nodePort: {{ .Values.service.nodePort | default nil }}
  selector:
    {{- include "my-nginx-serve.selectorLabels" . | nindent 4 }}
